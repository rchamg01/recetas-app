@(receta: Receta)
<receta>
    <nombre>@receta.getNombre</nombre>
    <tiempo>@receta.getTiempo</tiempo>
    <tipo>@receta.getTipo</tipo>
    @_usuario(receta.getUsuario)
    @descripcion(receta.getDescripcion)

    @ingredientes(receta.getIngredientes)

</receta>